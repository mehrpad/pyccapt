﻿name: pyccapt
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - pyccapt
      # SmarAct MCS2 stage SDK – NOT on PyPI, install manually:
      #   pip install "C:\SmarAct\MCS2\SDK\Python\packages\smaract.ctl-<version>.zip"
      # e.g.: pip install "C:\SmarAct\MCS2\SDK\Python\packages\smaract.ctl-1.5.2.zip"
